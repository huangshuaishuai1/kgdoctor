spring:
  application:
    name: doctor-service
  cloud:
    nacos:
      config:
        server-addr: 10.16.47.241:8848 #nacos中心地址
        file-extension: yaml # 配置文件格式
        shared-configs:
          - data-id: redis-config-dev.yaml
          - data-id: nacos-discovery-config-dev.yaml
  profiles:
    active: dev # 环境标识

